# Resend API key, scoped to THIS project (billie-website).
# Do NOT reuse the key the BiLLiE app uses for auth email.
# Real value lives in .env.local (gitignored) and in the Vercel project env.
RESEND_API_KEY=re_your_key_here
